apiVersion: v1
kind: Service
metadata:
  name: infra-monitoring-victoriametrics
  namespace: monitoring
spec:
  type: {{ .Values.service.type }}
  selector:
    app: infra-monitoring-victoriametrics
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.port }}