apiVersion: apps/v1
kind: Deployment
metadata:
  name: infra-monitoring-victorialogs
  namespace: monitoring
  labels:
    app: infra-monitoring-victorialogs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: infra-monitoring-victorialogs
  template:
    metadata:
      labels:
        app: infra-monitoring-victorialogs
    spec:
      nodeSelector:
        {{- toYaml .Values.nodeSelector | nindent 8 }}
      containers:
        - name: victorialogs
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --storageDataPath=/storage
            - --retentionPeriod={{ .Values.retentionPeriod }}
            - --httpListenAddr=:{{ .Values.port }}
          ports:
            - containerPort: {{ .Values.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: storage
              mountPath: /storage
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: infra-monitoring-victorialogs